# Work Log - 2026-05-24

Project: `C:\Development\projects\rust-text-editor`

## Summary

- Simplified the preview menu into a one-click toggle button.
- Added a project-specific `full-build` Codex skill and used it to rebuild Hotaru's release executable and installers.
- Improved Markdown preview rendering with Mermaid diagrams and a more readable preview color scheme.

## Completed Work

- Replaced the Preview dropdown menu with a pressed-state button that toggles preview visibility directly.
- Added `.codex/skills/full-build` with instructions for the full Hotaru production build path.
- Passed the active theme into Markdown preview rendering.
- Hardened fenced-code handling for `mermaid` and `json` blocks so custom renderers are not wrapped by the default code block.
- Updated Mermaid rendering to use theme-aware colors for light, dark, and system modes.
- Added preview-specific colors for headings, links, code blocks, tables, blockquotes, and diagram containers.
- Ran full release build and rebuilt:
  - `target/release/hotaru.exe`
  - `target/release/bundle/msi/Hotaru_0.1.0_x64_en-US.msi`
  - `target/release/bundle/nsis/Hotaru_0.1.0_x64-setup.exe`

## Files Changed

```text
M src/App.tsx
 M src/components/MarkdownPreview.tsx
 M src/components/MermaidBlock.tsx
 M src/styles.css
?? .codex/
?? docs/work-log/
```

## Verification

- `python C:/Users/chien/.codex/skills/.system/skill-creator/scripts/quick_validate.py .codex/skills/full-build` passed.
- `npm run build` passed after the preview menu change.
- Browser smoke test confirmed the Preview button toggles the preview pane on and off.
- `npm run build` passed after Mermaid and preview color changes.
- Browser smoke test confirmed a fenced `mermaid` block renders an SVG in preview.
- Browser smoke test confirmed `.mermaid-preview` is not nested inside a `<pre>` and JSON blocks are not double-wrapped.
- `npm run build` passed during final full-build.
- `npm run tauri build` passed during final full-build.
- Vite reported existing large-chunk warnings for dependencies; no build failures.

## Follow-ups

- A normal Git commit/push will not update GitHub Release download assets. The rebuilt MSI/NSIS artifacts must be uploaded to the release, or a release automation/tag flow must publish them.
- The local Vite browser preview still shows an existing Tauri-only `metadata` error when run outside the Tauri shell.
- Consider code splitting Mermaid/Excalidraw bundles later if the Vite chunk-size warnings become a release concern.

## Git Context

- Repository: yes
- Branch: `master`

### Diff Stat

```text
src/App.tsx                        | 27 ++++++------
 src/components/MarkdownPreview.tsx | 33 ++++++++++++---
 src/components/MermaidBlock.tsx    | 65 +++++++++++++++++++++++++++--
 src/styles.css                     | 84 ++++++++++++++++++++++++++++++++++++--
 4 files changed, 183 insertions(+), 26 deletions(-)
```

### Staged Diff Stat

```text
(none)
```

### Today's Commits

```text
(none)
```

## Commit And Push

- Commit: `168c2f3` (`wrap-up: preview improvements and full build`)
- Push: pushed `master` to `origin/master`

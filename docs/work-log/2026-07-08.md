# Work Log - 2026-07-08

Project: `C:\Development\projects\rust-text-editor`

## Summary

- Converted the project from the Hotaru/Vault desktop workflow into Koharu, a single-file Markdown text editor with Notepad-like file handling.
- Preserved the vault-oriented codebase in a separate worktree/branch for future work, then committed and pushed the Koharu implementation on `master`.
- Built and published Koharu download artifacts under a new GitHub Release tag, `koharu-v0.1.3`, without overwriting the existing Hotaru `v0.1.3` release.

## Completed Work

- Renamed the app/package/Tauri metadata to Koharu and regenerated icon assets from `asset/koharu.png`.
- Removed the vault sidebar from the Koharu UI and disabled vault commands from the Tauri invoke handler.
- Reworked the main app shell around single-file New/Open/Save/Save As/export/file-properties flows.
- Made Save As default to Markdown for new files.
- Restored English/Japanese UI switching and changed Koharu's dark theme palette so it is visually distinct from Hotaru.
- Restored the preview toggle button/icon in the menu bar.
- Fixed menus so clicking into the editor dismisses open menus and focuses typing.
- Fixed native window close behavior by routing close requests through the same close/unsaved-change flow used by the app.
- Added regression tests for file document behavior, menu dismissal, and close confirmation.
- Committed and pushed Koharu implementation as `35a9bd2`.
- Rebuilt the release artifacts and committed the generated build stamp as `367ca20`.
- Created and pushed tag `koharu-v0.1.3`.
- Created GitHub Release `Koharu v0.1.3` and uploaded `koharu.exe`, MSI, and NSIS installer assets.

## Files Changed

```text
See commits:
- 35a9bd2 feat: build Koharu single-file editor
- 367ca20 chore: update Koharu build stamp
```

## Verification

- `npm test` passed with 15 tests.
- `npm run build` passed.
- `npm run tauri build` passed and rebuilt:
  - `target/release/koharu.exe`
  - `target/release/bundle/msi/Koharu_0.1.3_x64_en-US.msi`
  - `target/release/bundle/nsis/Koharu_0.1.3_x64-setup.exe`
- Rendered browser QA verified the menu bar, preview toggle, Japanese UI switching, dark theme, and menu dismissal when clicking into the editor.
- Desktop smoke test launched `koharu.exe`, sent a normal close request, and verified the process exited.
- `gh release view koharu-v0.1.3 --repo KeithCCC/rust-text-editor` verified the release and uploaded assets.

## Follow-ups

- The Rust backend still contains unused vault helper functions, so release builds report dead-code warnings.
- If Koharu should become the primary product line, consider bumping the app version beyond `0.1.3` and cleaning up remaining Hotaru/vault references in docs and unused backend code.
- The old `v0.1.3` GitHub release remains a Hotaru release; Koharu downloads live under `koharu-v0.1.3`.

## Git Context

- Repository: yes
- Branch: `master`

### Diff Stat

```text
(none)
```

### Staged Diff Stat

```text
(none)
```

### Today's Commits

```text
367ca20 chore: update Koharu build stamp
35a9bd2 feat: build Koharu single-file editor
```

## Commit And Push

- Commit: pending wrap-up commit
- Push: pending wrap-up push

# Work Log - 2026-06-25

Project: `C:\Development\projects\rust-text-editor`

## Summary

- Improved Hotaru editor usability and performance around split preview, note search, and source-mode readability.
- Rebuilt the desktop app and installers after verifying no running `hotaru.exe` process was blocking the build.

## Completed Work

- Reorganized the current-note search UI into a clearer top-bar control with a label, wider query field, match count, Previous/Next actions, and Clear.
- Removed CodeMirror's bottom search panel from the editor path so users are not split between two different search interfaces.
- Added delayed preview/stat refresh behavior to reduce typing lag in split preview, especially on larger Markdown notes.
- Memoized Markdown preview rendering and refreshed the preview immediately when returning to split preview mode.
- Added source-mode heading styling for `#` through `####` so Markdown headings stand out in the edit pane.
- Updated app build metadata for the latest rebuild.

## Files Changed

```text
M src/App.tsx
 M src/buildInfo.ts
 M src/components/MarkdownEditor.tsx
 M src/components/MarkdownPreview.tsx
 M src/styles.css
```

## Verification

- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- `npm run tauri build`
- Rebuild pre-check: confirmed no running `hotaru.exe` process before packaging.

## Follow-ups

- Continue evaluating live preview and richer table editing separately from the default split editor workflow.
- Monitor preview responsiveness on very large notes after the debounce/memoization changes.

## Git Context

- Repository: yes
- Branch: `master`

### Diff Stat

```text
src/App.tsx                        | 153 ++++++++++++++++++++++++++++---------
 src/buildInfo.ts                   |   4 +-
 src/components/MarkdownEditor.tsx  |  34 ++++++++-
 src/components/MarkdownPreview.tsx |   6 +-
 src/styles.css                     | 114 +++++++++++++++++++++++----
 5 files changed, 255 insertions(+), 56 deletions(-)
```

### Staged Diff Stat

```text
(none)
```

### Today's Commits

```text
(none)
```

## Commit And Push

- Commit: this wrap-up commit (`wrap-up: improve editor search and preview performance`)
- Push: pushed `master` to `origin/master`
